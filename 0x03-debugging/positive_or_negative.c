nclude <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * positive_or_negative : function
 * @i : first operand
 * Retrun: 0
 */
void positive_or_negative(int i)
{

	if (i < 0)
	{
		printf("%d is negative", i);
	}
	return (0);
}
