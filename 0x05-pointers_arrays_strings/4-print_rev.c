#include "main.h"
#include <string.h>

/**
 * print_rev - function
 * @s : pointer
 * Return: 0
 */

void print_rev(char *s)
{
	int len;
	int i;

	for (i = l - 1; i >= 0; i--)
	{
		_putchar(str[i]);
	}
}
